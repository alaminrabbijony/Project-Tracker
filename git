
# Stage all changes (or use `git add filename.js` to add specific files)
git add . 

# Commit with a meaningful message                         
git commit -m "icon btn is stable; building camera" 

# Push to GitHub 
git push

# Revert to last commited githup repo
git restore .

# For new emulator
npx expo run:android

# Eas installation
npm install -g eas-cli@latest
eas --version

#to check eas login
eas whoami
# if not login
eas login

# if fails 
Remove-Item -Recurse -Force .git
git init
git add .
git commit -m "reset for eas build"
# to build the app
eas build -p android --profile development

# dor using insalled app in device
npx expo start --dev-client
